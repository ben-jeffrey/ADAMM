<Page x:Class="ADAMM.TeamScorePane"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ADAMM"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TeamScorePane">

    <Grid>
        <ListView x:Name="teamScores">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Team" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Score" DisplayMemberBinding="{Binding Score}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
